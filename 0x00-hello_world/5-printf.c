#include <stdio.h>
/**
   * main - print to screen using printf
   *
   * Return: zero
   */
int main(void)
{
char message[] = "with proper grammar, but the outcome is a piece of art,";
printf(message);
return (0);
}
